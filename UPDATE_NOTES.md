# Обновление от 2025-11-11

## ✨ Новые возможности

### 1. Оптимизация анимаций
- Заменены `transition: all` на конкретные свойства для лучшей производительности
- Упрощены сложные CSS трансформации (убраны одновременные rotate/rotateX/rotateY)
- Уменьшены значения blur в box-shadow
- Добавлены `will-change` и `translateZ(0)` для аппаратного ускорения
- Анимации теперь работают плавнее при переключении тем

### 2. Система лайков и дизлайков
- Добавлена возможность лайкать/дизлайкать треки
- Отображение количества лайков и дизлайков под информацией о треке в плеере
- Система запоминает реакцию пользователя (авторизованные и анонимные)
- Красивая анимация при клике на кнопки

### 3. Счетчик прослушиваний
- Автоматический подсчет прослушиваний (засчитывается через 30 секунд воспроизведения)
- Отображение количества прослушиваний в плеере

### 4. Система плейлистов (API готово, UI в разработке)
- API для создания/редактирования/удаления плейлистов
- API для добавления/удаления треков из плейлистов
- Поддержка публичных и приватных плейлистов

## 📦 Установка обновления

### Шаг 1: Обновление базы данных

Выполните SQL миграцию:

\`\`\`bash
mysql -u ваш_пользователь -p название_бд < database_migration.sql
\`\`\`

Или выполните команды вручную через phpMyAdmin/другой SQL клиент, открыв файл `database_migration.sql`.

### Шаг 2: Очистка кеша браузера

После обновления рекомендуется очистить кеш браузера (Ctrl+F5 или Cmd+Shift+R).

## 📝 Изменённые файлы

### JavaScript
- `assets/js/theme-system-v2.js` - оптимизация анимаций
- `assets/js/epic-player.js` - добавлены события для статистики
- `assets/js/track-stats.js` - **новый файл** для работы со статистикой треков

### CSS
- `assets/css/epic-player.css` - добавлены стили для статистики

### PHP (API)
- `api/player/track-reaction.php` - **новый** API для лайков/дизлайков
- `api/player/track-view.php` - **новый** API для подсчета прослушиваний
- `api/player/track-stats.php` - **новый** API для получения статистики
- `api/player/playlist-manage.php` - **новый** API для управления плейлистами
- `api/player/playlist-tracks.php` - **новый** API для управления треками в плейлистах

### HTML
- `pages/player.php` - добавлено отображение статистики

### SQL
- `database_migration.sql` - **новый** файл с миграцией БД

## 🎯 Как использовать

### Лайки и дизлайки
1. Откройте любой трек в плеере
2. Под информацией о треке появятся кнопки 👍 и 👎
3. Нажмите на кнопку, чтобы поставить лайк или дизлайк
4. Повторное нажатие уберет вашу реакцию

### Прослушивания
- Счетчик обновляется автоматически после 30 секунд воспроизведения
- Отображается рядом с иконкой 👁️

### Плейлисты (в разработке)
- API готово, UI будет добавлено в следующем обновлении

## 🐛 Известные проблемы
Нет

## 📊 База данных

### Новые таблицы:
- `Playlists` - плейлисты пользователей
- `PlaylistTracks` - связь плейлистов и треков
- `TrackReactions` - реакции пользователей на треки

### Новые поля в Track:
- `likes` (INT) - количество лайков
- `dislikes` (INT) - количество дизлайков

## 🚀 Следующие обновления

- UI для создания и управления плейлистами
- Возможность делиться плейлистами
- Топ треков по лайкам/прослушиваниям
- Рекомендации на основе лайков
