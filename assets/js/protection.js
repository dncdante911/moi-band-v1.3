/**
 * ═══════════════════════════════════════════════════════════════
 * MASTER OF ILLUSION - PROTECTION SYSTEM v2.0
 * ═══════════════════════════════════════════════════════════════
 *
 * v2.0: убраны блокировки F12/ПКМ/Ctrl+U — они не останавливают
 * никого мотивированного (DevTools открывается ещё десятком способов),
 * зато реально мешают обычным посетителям (нельзя открыть ссылку в
 * новой вкладке, нельзя сохранить страницу). Настоящая защита треков
 * теперь на сервере — см. include_config/StreamToken.php: ссылки на
 * аудио подписаны и истекают по времени, их нельзя скопировать и
 * использовать позже или расшарить как прямую ссылку.
 *
 * Тут остаётся то, что не мешает пользователю: копирайт-баннер в
 * консоли и лёгкое предупреждение при явной попытке скопировать код
 * страницы.
 *
 * © 2025 Master of Illusion | moi-band.com.ua
 * Все права защищены
 *
 * ═══════════════════════════════════════════════════════════════
 */

(function() {
    'use strict';

    // ═══════════════════════════════════════════════════════════════
    // 🎨 КРАСИВОЕ ПРЕДУПРЕЖДЕНИЕ В КОНСОЛИ
    // ═══════════════════════════════════════════════════════════════

    function showConsoleWarning() {
        // Огромное предупреждение
        console.log(
            '%c⚠️ ВНИМАНИЕ! ⚠️',
            'color: #ff0000; font-size: 50px; font-weight: bold; text-shadow: 3px 3px 0 #000, -1px -1px 0 #FFD700;'
        );
        
        console.log(
            '%c🔒 ЗАЩИЩЕННАЯ ЗОНА',
            'color: #FFD700; font-size: 30px; font-weight: bold; font-family: "Cinzel Decorative", serif;'
        );
        
        console.log(
            '%cЭтот сайт защищен авторским правом!',
            'color: #FFA500; font-size: 20px; font-weight: bold; margin-top: 10px;'
        );
        
        console.log(
            '%c© 2025 Master of Illusion\n' +
            '🎸 Power Metal Project\n' +
            '🌐 https://moi-band.com.ua',
            'color: #ccc; font-size: 16px; line-height: 1.8; margin-top: 15px;'
        );
        
        console.log(
            '%c⚖️ ПРАВОВАЯ ИНФОРМАЦИЯ:',
            'color: #ff6b6b; font-size: 18px; font-weight: bold; margin-top: 20px;'
        );
        
        console.log(
            '%c• Несанкционированное копирование кода запрещено\n' +
            '• Использование материалов только с разрешения автора\n' +
            '• Нарушение авторских прав преследуется по закону\n' +
            '• Все попытки взлома логируются и отслеживаются',
            'color: #fff; font-size: 14px; line-height: 2; background: rgba(255, 0, 0, 0.1); padding: 10px; border-left: 4px solid #ff0000;'
        );
        
        console.log(
            '%c💡 Хочешь что-то подобное?\n' +
            'Свяжись с разработчиком!',
            'color: #00ff00; font-size: 16px; font-weight: bold; margin-top: 20px;'
        );
        
        console.log(
            '%c═══════════════════════════════════════════════════════',
            'color: #FFD700; font-size: 12px;'
        );
    }
    
    // Показываем предупреждение при загрузке
    showConsoleWarning();

    // F12 / ПКМ / Ctrl+U и детект открытия DevTools сознательно убраны —
    // они не мешают никому, кто реально хочет скачать трек или скопировать
    // код (curl, "Просмотр кода" через адресную строку, отключение JS —
    // всё это работает независимо от таких блокировок), а обычным
    // посетителям ломают штатные вещи браузера (открыть в новой вкладке,
    // сохранить страницу, воспользоваться инспектором для доступности).
    // Реальная защита треков — на сервере, см. include_config/StreamToken.php.

})();

/**
 * ═══════════════════════════════════════════════════════════════
 * КОНЕЦ ФАЙЛА ЗАЩИТЫ
 * ═══════════════════════════════════════════════════════════════
 */