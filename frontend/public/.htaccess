Options -MultiViews
RewriteEngine On
RewriteBase /

# Отдаём существующие файлы и папки как есть
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Всё остальное → index.html (Vue Router)
RewriteRule ^ index.html [L]
